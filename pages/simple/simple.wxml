<!--pages/simple/simple.wxml-->
<view class="container">
  <view class="page-section">
    <view class="weui-cells weui-cells_after-title clearfix">
      <view class="weui-cell weui-cell_input fl">
        <image class="search-ico" src="../../image/search.png"></image>
        <input class="weui-input" auto-focus placeholder="请输入关键词" />
      </view>
      <view class="weui-r fr" bindtap="goAddSimple">
        <image src="../../image/inp-r.png"></image>
      </view>
    </view>
  </view>
  <view class="list-box">
    <view wx:for="{{dataList}}" wx:key="{{item.id}}" class="item-list">
      <view class="item-title">{{item.Title}}</view>
      <view class="simple-title">发布人：{{item.Publisher}}</view>
      <view class="item-con">
        {{item.Desc}}
      </view>
      <view class="some-time">发布时间：</view>
    </view>
  </view>
</view>